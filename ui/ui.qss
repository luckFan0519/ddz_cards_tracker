/* style.qss
   UI 样式集中管理：方便维护、方便换主题
   -------------------------------------------------
   说明：
   - 牌名 QLabel：objectName = "CardNameLabel"
   - 数量 QLabel：objectName = "CardCountLabel"
   - depleted 状态：dynamicProperty depleted=true/false
     通过 QSS 属性选择器匹配：
     QLabel#CardNameLabel[depleted="true"] { ... }
*/

/* -----------------------------
   全局默认：让字体更统一一点
   ----------------------------- */
QWidget {
    font-family: "Microsoft YaHei", "PingFang SC", "Noto Sans CJK SC";
    font-size: 14px;
}

/* -----------------------------
   牌名：默认样式
   ----------------------------- */
QLabel#CardNameLabel {
    font-size: 14px;
    font-weight: 600;
    color: #111;
}

/* depleted：牌名变灰（对应你原来 v<=0 的 setStyleSheet）
   你原样式 color:#999; 这里完全照做 */
QLabel#CardNameLabel[depleted="true"] {
    font-size: 14px;
    font-weight: 600;
    color: #999;
}

/* -----------------------------
   数量：默认样式
   ----------------------------- */
QLabel#CardCountLabel {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    background: #f4f4f4;
    color: #111;
}

/* depleted：数量变灰（对应你原来 v<=0 的 setStyleSheet） */
QLabel#CardCountLabel[depleted="true"] {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    background: #f4f4f4;
    color: #999;
}

/* 牌名：默认样式（无边框） */
QLabel#CardNameLabel {
    font-size: 14px;
    font-weight: 600;
    color: #111;
    padding: 2px 6px;
    background: transparent;
}

/* 牌名 depleted：无边框，只变色 */
QLabel#CardNameLabel[depleted="true"] {
    font-size: 14px;
    font-weight: 600;
    color: #999;
    padding: 2px 6px;
    background: transparent;
}

/* 数量：默认样式（无边框） */
QLabel#CardCountLabel {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    background: #f4f4f4;
    color: #111;
}

/* 数量等于4时显示为淡红色 */
QLabel#CardCountLabel[count="4"] {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    background: #f4f4f4;
    color: #ff6666;
}

/* 数量 depleted：无边框，只变色 */
QLabel#CardCountLabel[depleted="true"] {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    background: #f4f4f4;
    color: #999;
}
